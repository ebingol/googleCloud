# OQ-04 — Eventarc Standard: CloudEvents, Audit Logs and Pub/Sub

5 soru. Her soruda seçilecek cevap sayısı belirtilmiştir.

**Kaynak türü:** Paylaştığın Choreography and Orchestration quizi ve belirtilen resmî dokümanlar. Bu konular mevcut Introduction to Microservices PDF’sinde anlatılmıyor.

## 1. Which event-format statement corrects the mistake in your submitted quiz?

**Select one.**

- A. Only events from the same producer need a shared format
- B. Eventarc delivers events in the standard CloudEvents format
- C. Any arbitrary format is acceptable if events of the same type match
- D. Each receiver invents an unrelated envelope format

## 2. Which two statements form the expected correct pair for your Eventarc question in its Standard context?

**Select two.**

- A. Any per-type custom event format is sufficient
- B. Pub/Sub is the transport layer
- C. Consumers must monitor an event queue themselves
- D. Cloud Audit Logs entries can be event sources

## 3. An Audit Logs entry is written. Under what condition does the described Eventarc trigger route it?

**Select one.**

- A. Every log entry must always be sent to every destination
- B. It matches the trigger's filter criteria
- C. The event producer embeds every downstream service's implementation
- D. A developer manually copies it to the receiver

## 4. For a Cloud Run event receiver, which interaction matches Eventarc delivery?

**Select one.**

- A. Every producer must call the receiver's internal functions
- B. The receiver must directly scan all source audit-log files
- C. Application code must continuously poll Eventarc's event queue
- D. Eventarc routes matching events to the configured receiver

## 5. Which pairing correctly separates event representation from transport in this course context?

**Select one.**

- A. Cloud Audit Logs: transport for all events; CloudEvents: destination identity
- B. CloudEvents: database engine; Pub/Sub: authentication token
- C. CloudEvents: event format; Pub/Sub: transport
- D. Pub/Sub: event-format standard; CloudEvents: transport queue

## Cevapların

`1: … | 2: … | 3: … | 4: … | 5: …`

Güven düzeyi: emin / kararsız / tahmin.

[Cevap anahtarı](../answers/orchestration/OQ-04.md) · [Bölüm dizini](README.md) · [Tüm quizler](../README.md)
