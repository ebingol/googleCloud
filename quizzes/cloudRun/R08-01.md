# R08-01 — Environment variables and configuration precedence

5 soru. Sorular belirtilen ders PDF’sinin anlatımını esas alır; sürüme bağlı limitler ve ayarlar güncel ürün garantisi değildir. Her soruda seçilecek cevap sayısı belirtilmiştir.

**Kaynak:** [T-DVCRUN-B-m2-l5-file-en-11.en.pdf](../../cloudRun/T-DVCRUN-B-m2-l5-file-en-11.en.pdf)

## 1. How are Cloud Run environment settings represented?

**Select one.**

- A. Key-value pairs injected into the application container
- B. Only encrypted TLS certificates
- C. Only image registry tag names
- D. Only source-code comments

## 2. Which value wins when a Cloud Run setting has the same name as a Dockerfile ENV default?

**Select one.**

- A. The variable is automatically deleted
- B. A random value is chosen on each request
- C. The Cloud Run service/job setting
- D. The Dockerfile always overrides deployment settings

## 3. Which Python expression reads an environment variable in the lesson?

**Select one.**

- A. process.env.key
- B. docker.inspect('key')
- C. os.environ.get('key')
- D. System.getenv('key')

## 4. Which two language/accessor pairings match the PDF?

**Select two.**

- A. Java: os.environ.get()
- B. Python: process.env
- C. Java: System.getenv()
- D. Node.js: process.env

## 5. Can every possible environment-variable name be set by the user?

**Select one.**

- A. Only names also used as image tags may be set
- B. No; the runtime contract reserves some names
- C. Yes; there are never any reserved variables
- D. Only variables beginning with every project's ID are permitted

## Cevapların

`1: … | 2: … | 3: … | 4: … | 5: …`

Güven düzeyi: emin / kararsız / tahmin.

[Cevap anahtarı](../answers/cloudRun/R08-01.md) · [Bölüm dizini](README.md) · [Tüm quizler](../README.md)
