# U03-04 — VPC routing, firewall rules, peering and Shared VPC

5 soru. Soruların her birinde kaç seçenek seçileceği belirtilmiştir.

## 1. Must a user provision a separate router for the built-in VPC routing tables?

**Select one.**

- A. Yes; internal traffic always requires an external IP
- B. No; routing tables are built into VPC networking
- C. Yes; every inter-zone route needs a customer-managed router VM
- D. Yes; a physical router for each subnet is mandatory

## 2. Which two traffic directions can the distributed VPC firewall control?

**Select two.**

- A. Outgoing traffic
- B. Only database query plans
- C. Incoming traffic
- D. Only build pipeline stages

## 3. What can target a firewall rule at all web-server VMs regardless of their IP addresses in the example?

**Select one.**

- A. A billing-account label
- B. A network tag such as WEB
- C. A container image tag alone
- D. A Cloud Storage object prefix

## 4. Which feature establishes a relationship between two VPC networks to exchange traffic?

**Select one.**

- A. VPC Network Peering
- B. Shared VPC host/service-project attachment
- C. Cloud CDN cache fill
- D. Cloud Router as a replacement for all VPC connections

## 5. Which feature lets one project use another project's VPC under IAM-based control?

**Select one.**

- A. A Cloud Storage bucket ACL
- B. VPC Peering as shared ownership of one network
- C. Shared VPC
- D. A Cloud DNS public zone

## Cevapların

`1: … | 2: … | 3: … | 4: … | 5: …`

Güven düzeyi: emin / kararsız / tahmin.

[Cevap anahtarı](../answers/fundamentals/U03-04.md) · [Quiz dizini](../README.md)
